# Governed deterministic moderation terms (UTF-8).
# Format: CATEGORY|LANGUAGE|TERM
#
# Only add high-precision terms that are safe to block without context. Matching
# is Unicode-normalized and uses word/phrase boundaries. Contextual words belong
# in the AI policy, not this file. Changes require review and a service restart.

# An empty term set is valid. Add only reviewed, high-precision production rules.
