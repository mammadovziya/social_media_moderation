COMMENT ON COLUMN moderation_username_decision_audit_events.blocked_terms_digest IS
    'Versioned semantic SHA-256 of the request-scoped blocked-terms snapshot; current v3 binds the username vulgar-fold profile and category scope';
