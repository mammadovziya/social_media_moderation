package com.example.moderation.gateway.api;

/** Investment-community relevance of the current content in its conversation context. */
public enum Domain {
    INVESTMENT_RELATED,
    INVESTMENT_ADJACENT,
    OFF_TOPIC,
    UNCERTAIN
}
